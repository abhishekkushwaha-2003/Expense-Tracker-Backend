server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:mysql://localhost:3306/spendsmart_auth_db
    username: root
    password: Abhishek@2003
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: kushwahaabhishek10k@gmail.com
    password: "wogm jnvp bzdq ydqt"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

  data:
    redis:
      repositories:
        enabled: false
      host: localhost
      port: 6379
      timeout: 5s

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

app:
  otp:
    expiry-minutes: 10
    from-email: kushwahaabhishek10k@gmail.com
    redis-enabled: false
  messaging:
    notification:
      async-enabled: false
      exchange: spendsmart.notification.exchange
      routing:
        send: notification.send

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

  instance:
    hostname: localhost      
    prefer-ip-address: true
