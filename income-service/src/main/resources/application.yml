server:
  port: 8083

spring:
  application:
    name: income-service

  datasource:
    url: jdbc:mysql://localhost:3306/spendsmart_income_db
    username: root
    password: Abhishek@2003
    driver-class-name: com.mysql.cj.jdbc.Driver  

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true               
    fetch-registry: true                       

  instance:
    hostname: localhost
    prefer-ip-address: true